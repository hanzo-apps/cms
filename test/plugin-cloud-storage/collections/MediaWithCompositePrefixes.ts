import type { CollectionConfig } from '@hanzo/cms'

export const MediaWithCompositePrefixes: CollectionConfig = {
  slug: 'media-with-composite-prefixes',
  upload: {
    disableLocalStorage: true,
  },
  fields: [],
}
